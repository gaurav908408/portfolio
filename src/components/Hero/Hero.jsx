function Hero() {
  return (
    <section
      id="home"
      style={{
        height: "100vh",
        display: "grid",
        placeItems: "center",
        fontSize: "40px"
      }}
    >
      Hero Section
    </section>
  );
}

export default Hero;